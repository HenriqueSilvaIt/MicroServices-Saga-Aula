server:
  port: 3000

spring:
  kafka:
    bootstrap-servers: ${KAFKA_BROKER:172.22.79.10:9092}
    topic:
      start-saga: start-saga
      notify-ending: notify-ending
    consumer:
      group-id: order-group
      auto-offset-reset: latest

  data:
    mongodb:
      database: admin
      uri: ${MONGO_DB_URI:mongodb://admin:123456@172.22.79.10:27017}
      #eu passei ao invés do localhost passei o ip do wsl 172.22.79.10 para conectar ao mongo

logging:
  level:
    org:
      apache:
        kafka: OFF